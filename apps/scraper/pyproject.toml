[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "fb-bot-scraper"
version = "0.1.0"
description = "FB Marketplace Bot - Scraper Service"
requires-python = ">=3.12"
